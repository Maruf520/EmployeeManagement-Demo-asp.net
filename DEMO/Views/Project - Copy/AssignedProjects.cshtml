@model DEMO.Models.EmployeeProjectModel

@{
    ViewData["Title"] = "AssignedProjects";
}

<h1>AssignedProjects</h1>

<div>
    <h4>EmployeeProject</h4>
    <hr />
    <dl class="row">
        @foreach (var Emp in ViewBag.project)
        {

                @Emp.ProjectName

          
            <dd class="col-sm-10">
                @foreach (var proj in ViewBag.project)
                {
                   @proj.LastName
                }

            </dd>

        }

    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
